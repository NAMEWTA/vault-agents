name: Release

on:
  push:
    tags:
      - '*.*.*'

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-rust:
    name: Build Rust ${{ matrix.platform }}-${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win32
            arch: x64
            target: x86_64-pc-windows-msvc
            ext: .exe
          - os: macos-latest
            platform: darwin
            arch: arm64
            target: aarch64-apple-darwin
            ext: ''
          - os: macos-latest
            platform: darwin
            arch: x64
            target: x86_64-apple-darwin
            ext: ''
          - os: ubuntu-latest
            platform: linux
            arch: x64
            target: x86_64-unknown-linux-gnu
            ext: ''
          - os: ubuntu-24.04-arm
            platform: linux
            arch: arm64
            target: aarch64-unknown-linux-gnu
            ext: ''

    steps:
      - uses: actions/checkout@v5

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust-servers -> target
          key: ${{ matrix.target }}

      - name: Build
        working-directory: rust-servers
        env:
          TERMINAL_SERVER_VERSION: ${{ github.ref_name }}
        run: cargo build --release --target ${{ matrix.target }}

      - name: Prepare binary
        shell: bash
        run: |
          mkdir -p binaries
          BINARY_NAME="vault-agents-server-${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.ext }}"
          cp "rust-servers/target/${{ matrix.target }}/release/vault-agents-server${{ matrix.ext }}" "binaries/${BINARY_NAME}"
          
          cd binaries
          if command -v sha256sum &> /dev/null; then
            sha256sum "${BINARY_NAME}" > "${BINARY_NAME}.sha256"
          else
            shasum -a 256 "${BINARY_NAME}" > "${BINARY_NAME}.sha256"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: server-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            binaries/vault-agents-server-${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.ext }}
            binaries/vault-agents-server-${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.ext }}.sha256

  release:
    name: Release
    needs: build-rust
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      id-token: write
      attestations: write

    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Download binaries
        uses: actions/download-artifact@v7
        with:
          pattern: 'server-*'
          path: binaries
          merge-multiple: true

      - name: Set permissions
        run: |
          chmod +x binaries/vault-agents-server-darwin-* || true
          chmod +x binaries/vault-agents-server-linux-* || true

      - name: Install and build
        run: |
          pnpm install --frozen-lockfile
          pnpm build

      - name: Generate release notes
        run: |
          node scripts/release-notes.js \
            --version "${{ github.ref_name }}" \
            --repository "${{ github.repository }}" \
            --output release-body.md \
            || printf 'Vault Agents %s\n' "${{ github.ref_name }}" > release-body.md

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            main.js
            styles.css

      - name: Prepare release assets
        run: |
          chmod +x binaries/vault-agents-server-darwin-* || true
          chmod +x binaries/vault-agents-server-linux-* || true
          rm -rf vault-agents
          mkdir -p vault-agents
          cp main.js manifest.json styles.css vault-agents/
          zip -r "vault-agents-${{ github.ref_name }}.zip" vault-agents

      - name: Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            vault-agents-${{ github.ref_name }}.zip
            binaries/vault-agents-server-win32-x64.exe
            binaries/vault-agents-server-win32-x64.exe.sha256
            binaries/vault-agents-server-darwin-arm64
            binaries/vault-agents-server-darwin-arm64.sha256
            binaries/vault-agents-server-darwin-x64
            binaries/vault-agents-server-darwin-x64.sha256
            binaries/vault-agents-server-linux-x64
            binaries/vault-agents-server-linux-x64.sha256
            binaries/vault-agents-server-linux-arm64
            binaries/vault-agents-server-linux-arm64.sha256
          body_path: release-body.md
          draft: false
          prerelease: false
